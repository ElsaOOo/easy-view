import { createStore } from "vuex";
import elePlusForm from "./modules/ele-plus-form";

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    elePlusForm,
  },
});
